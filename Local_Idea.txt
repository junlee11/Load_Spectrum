Drive : C / D / E 선택 → 없으면 인터락
확장자 별로 정렬 / 폴더 먼저
변수 저장 : 현재 경로 / 이전 경로 / 앞의 경로

back을 클릭하면 back_flag +1 하고 현재의 경로를 앞의 경로에 저장 / back_flag -1
self.c_cur_path / self.c_back_path / self.c_fow_path 
self.c_back_path 
self.c_fow_list
1. self.c_cur_path가 drive이면 back 불가 
2. back 클릭하면 이전 경로로 이동, back_flag +1, self.c_fow_list.append(self.c_cur_path)
3. foward 클릭하면 앞의 경로로 이동, back_flag -1, self.c_fow_list.pop()

drive 클릭하면 리스트 뜨도록, 폴더 더블클릭하면 해당 경로로 이동, 파일이면 실행

멀티선택 가능 
드롭만 가능하도록

추후 ftp 업로드 기능 추가